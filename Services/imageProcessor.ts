
// Este servicio ha sido desactivado por solicitud del usuario para simplificar el flujo de imágenes.
// Las imágenes se guardan ahora directamente sin procesamiento de logo.

export async function processProductImage(base64Source: string): Promise<string> {
  return Promise.resolve(base64Source);
}
